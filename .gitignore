# Mac OS
.DS_Store

# NPM
/node_modules
npm-*

# Testing
/.nyc_output
/coverage

# Build
/build
/dist
/.opt-in

# Generated translation files
/translations
/locale

# Other generated source
# /src/generated — tracked: stub committed so Railway doesn't need to run prepublish

# Downloaded during "npm install"
# /static/microbit — tracked: stub committed so Railway doesn't need to run prepublish

# for act
.secrets
